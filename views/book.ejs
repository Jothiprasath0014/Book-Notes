<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Notes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  
  <style>
    :root {
        --bg-oxford-blue: hsla(222, 44%, 13%, 1);
    }

    .container {
      background-color: #f5f5f5; /* Light gray background */
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      max-width: 800px;
      margin: 0 auto;
    }
    .form-container {
      background-color: #ffffff; /* White background for form container */
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .full-width {
      width: 100%;
      padding: 10px;
      border: none;
      border-radius: 5px;
      background-color: #007bff;
      color: white;
    }
    .full-width:hover {
      background-color: var(--bg-oxford-blue);
    }
    ::-webkit-scrollbar {
        width: 15px;
    }
    ::-webkit-scrollbar-track {
        background-color: transparent;
    }
    ::-webkit-scrollbar-thumb {
        background-color: lightblue;
        --radius-pill: 200px;
    }
    .footer {
        background-color: #fff8f8;
        color: #fff;
        padding: 1rem 0;
    }
    .footer span.text-muted {
        color: #fff;
    }
    .social-list {
        display: flex;
        align-items: center;
        list-style: none;
        padding: 0;
    }
    .social-list li {
        margin-right: 1rem;
    }
    .social-link {
        display: flex;
        align-items: center;
        color: #434343;
        text-decoration: none;
        transition: color 0.3s ease;
    }
    .social-link ion-icon {
        font-size: 1.5rem;
        margin-right: 0.5rem;
        transition: color 0.3s ease;
    }
    .social-link:hover ion-icon {
        color: currentColor;
    }
    .social-link:hover .span {
        color: currentColor;
    }
    .social-link:hover {
        color: initial;
    }
    .social-link:hover[href*="github"] {
        color: #333;
    }
    .social-link:hover[href*="linkedin"] {
        color: #0077b5;
    }
    .social-link:hover[href*="mail"] {
        color: #d44638;
    }
    </style>
</head>

<body>
  <div class="container col-xxl-7 px-4 pt-5">
    <div class="form-container">
      <% if (locals.book) { %>
        <h1 class="text-center"> <%= heading %> for Book: <span style="font-style: italic;"> '<%= book.title %>' </span></h1>

        <div class="align-items-center g-1 pt-2 pb-5 mb-5 mt-1">
          <form name="editForm" id="editForm" method="POST" action="/booknotes/<%= book.id %>">
            <div class="mb-3">
              <label for="notes" class="form-label">Notes:</label>
              <textarea name="notes" id="notes" class="form-control" rows="5"><%= book.notes %></textarea>
            </div>

            <div class="mb-3">
              <label for="rating" class="form-label">Rating:</label>
              <select name="rating" id="rating" class="form-control">
                <% for (var i = 1; i <= 10; i++) { %>
                  <% if (book.rating === i) { %> 
                    <option value="<%= i %>" selected><%= i %></option>
                  <% } else { %>
                    <option value="<%= i %>"><%= i %></option>
                  <% } %>
                <% } %>
              </select>
            </div>
            
            <div class="mb-3">
              <label for="date_read" class="form-label">Date Read:</label>
              <input name="date_read" id="date_read" class="form-control" type="date" value="<%= book.date_read %>" min="1900-01-01" max="<%= (new Date()).toISOString().split('T')[0] %>" />
            </div>

            <div class="mb-3">
              <button class="full-width" type="submit"><%= submit %></button>
            </div>
          </form>
        </div>
      <% } else { %>
        <h1 class="text-center"> <%= heading %></h1>

        <div class="align-items-center g-1 pt-2 pb-5 mb-5 mt-1">
          <form name="editForm" id="editForm" method="POST" action="/booknotes/">
            <div class="mb-3">
              <label for="title" class="form-label">Title:</label>
              <input name="title" id="title" type="text" class="form-control" placeholder="Book Title" size="50" required>
            </div> 
   
            <div class="mb-3">
              <label for="rating" class="form-label">Rating:</label>
              <select name="rating" id="rating" class="form-control">
                <% for (var i = 1; i <= 10; i++) { %>
                  <option value="<%= i %>"><%= i %></option>
                <% } %>
              </select>
            </div>

            <div class="mb-3">
              <label for="isbn" class="form-label">ISBN:</label>
              <input name="isbn" id="isbn" type="text" class="form-control" size="50" placeholder="ISBN will be needed to display Book's Cover">
            </div>
               
            <div class="mb-3">
              <label for="date_read" class="form-label">Date Read:</label>
              <input required name="date_read" id="date_read" class="form-control" type="date" min="1900-01-01" max="<%= (new Date()).toISOString().split('T')[0] %>" />
            </div>

            <div class="mb-3">
              <label for="notes" class="form-label">Notes:</label>
              <textarea name="notes" id="notes" class="form-control" rows="5" placeholder="Add a note about what you felt and what you learnt while reading this book."></textarea>
            </div>
   
            <div class="mb-3">
                <!--onclick function can return the successfull Message-->
              <button class="full-width" type="submit" onclick="return confirm('Added Successful');"><%= submit %></button> 
            </div>
          </form>
        </div>
      <% } %>  
    </div>
  </div>
   
  <!-- Footer -->

   <footer class="footer mt-auto py-3 bg-semibold">
    <div class="container d-flex justify-content-between align-items-center">
      <span class="text-muted">&copy; <%= new Date().getFullYear() %> 
        Jothiprasath P A. All rights reserved.
      </span>
  
      <ul class="social-list d-flex list-unstyled mb-0">
        <li class="mx-2">
            <a href="https://github.com/Jothiprasath0014/Jothiprasath0014" class="social-link">
            <ion-icon name="logo-github"></ion-icon>
            <span class="span">GitHub</span>
          </a>
        </li>
  
        <li class="mx-2">
            <a href="linkedin.com/in/jothi-prasath-2081571b1" class="social-link">
            <ion-icon name="logo-linkedin"></ion-icon>
            <span class="span">LinkedIn</span>
          </a>
        </li>
  
        <li class="mx-2">
            <a href="mailto:jothiprasathpa@gmail.com" class="social-link">
            <ion-icon name="mail-outline"></ion-icon>
            <span class="span">Mail</span>
          </a>
        </li>
      </ul>
    </div>
  </footer>

  <!-- Bootstrap JS for interactive components -->

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  
  <!-- Ionicons for social media icons -->
  <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
  <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
</body>

</html>
